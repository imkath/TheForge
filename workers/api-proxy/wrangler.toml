name = "theforge-api"
main = "index.ts"
compatibility_date = "2024-01-01"

# Workers configuration
workers_dev = true

# Routes (will be configured after connecting domain)
# routes = [
#   { pattern = "api.theforge.dev/*", zone_name = "theforge.dev" }
# ]

[build]
command = ""

# Environment variables (if needed)
# [vars]
# API_KEY = "your-key"
